apply plugin: "war"

bootWar {
    mainClass = "com.dazzle.asklepios.BillingApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
